name: Install ruby dependencies
description: This installs necessary gems

runs:
  using: composite
  steps:
    - name: Install Ruby and gems
      uses: ruby/setup-ruby@v1
      with:
        bundler-cache: true
        ruby-version: 3.0.2